# 螺丝消除 · 解压小游戏

> v3.3 | 2026-08-11

## 项目简介

基于抖音爆款 **Screw Jam（螺丝消除）** 玩法复刻的 HTML5 解压小游戏。
单文件实现，Canvas 渲染 + Web Audio API 音效合成，无任何外部依赖。

## 技术栈

- **前端**: 原生 HTML/CSS/JS，单文件 `index.html`
- **渲染**: Canvas 2D（六角螺丝、面板、粒子）
- **音效**: Web Audio API（全合成，无音频文件）
- **部署**: GitHub Pages（根目录）

## 部署方式

GitHub Pages → 仓库 Settings → Pages → Branch: `master` → 根目录 → Save

URL: https://z15314102792-arch.github.io/screw-jam/

## 核心架构

```
index.html
├── Audio (音效引擎) — Web Audio API 合成
│   ├── unscrew / boxIn / boxFull / panelDrop / win / lose / bgm
├── genLevel(lv) — 关卡生成器
│   ├── 2~6 色 × 3~4 螺丝/盒 = 6~24 螺丝
│   ├── 面板随机布局 + 层级遮挡
│   └── 收纳盒队列随机排列
├── Game (游戏主类)
│   ├── _tap → _remove → _animUn → _place → _checkPanels
│   ├── undo / hint / restart / next
│   └── _loop (60fps 渲染)
└── 粒子系统: sparks / burst / panelBurst
```

## 版本历史

### v3.3 (2026-08-11) — 覆盖率稳定性修复（契约验收）
- **锚点聚类面板布局** — 面板不再纯随机散布，改为1-3个锚点周围聚集，簇内面板可靠重叠
- **强制重叠螺丝放置** — findCoverTargets 空时不再退化为网格散布，而是搜索任意重叠下层板并在其螺丝附近放置
- **覆盖率门槛** — genLevel 新增最低覆盖率检查，不达标则丢弃重试（Lv1-3≥25%, Lv4-7≥35%, Lv8-13≥45%, Lv14+≥50%）
- **契约验收通过**：
  - M1 同级稳定性：max stddev=14（目标≤25）
  - M2 低关底线：min coverage=33%（目标≥25%）
  - M3 递进趋势：early_avg=43% < mid_avg=54%
  - M4 可解性：90/90=100%

### v3.2 (2026-08-11) — 紧急修复：画面空白
- **修复 shakeX 未定义导致 `_draw()` 崩溃** — v3.1 清理震动系统时漏删 `_draw()` 中两处 `GX+shakeX`/`GY+shakeY` 引用，导致 JS 报错后整个渲染中断，Canvas 一片空白。改为直接使用 `GX`/`GY`。

### v3.1 (2026-08-11) — 审计全面修复
- **P0: 面板分布恢复** — spread 0.22→0.42，面板不再挤在中心。空间层级排序回退为创建顺序（在小区域内Y排序无意义）。
- **审计 C~R 共12项修复**：
  - C: `_drawScrewRaw` 加 save/restore 保护
  - D: 删除废代码（无用渐变+fillStyle）
  - E: `_drawScrew` 移除未使用的 `blocked` 参数（3处调用站点清理）
  - F: 删除震动系统死代码
  - H: 关卡兜底重试 150→300 次 + 降低 tempMax 的 fallback
  - L: 解算器改进 — 贪心卡住时评估每颗螺丝的"释放潜力"，选最优
  - O: `_isScrewBlocked` 检查 `dropStart`，面板掉落动画期间不再阻挡
  - P: 统一收纳目标 Y 坐标（`_getTarget` 82→80，与 `_autoClearTemp` 一致）
  - R: `_bgmLoop` 开头清空 `_bgmNodes` 数组防内存泄漏

### v3.0 (2026-08-11) — 根因修复版
- **代码审计驱动**：5维度22个问题，修复3个根因：
  1. **螺丝级阻塞** — `_blocked(面板级)→_isScrewBlocked(螺丝级)`，每颗螺丝独立判断是否被上层板形状覆盖。露出来就能点，盖住才是ghost。解算器/hint/tap/draw 全线同步。
  2. **空间层级** — 面板layer按cy坐标排列（Y越小=越靠上=layer越大），视觉层序=逻辑层序，不再有"底层板盖上层板"的矛盾。
  3. **形状感知阻断** — 阻断检测改用 `shapeContains` 替代 AABB，圆形/三角/六角/矩形精确判定。
- **BGM叠加修复** — `bgm()` 先 `stopBGM()` 再 `_bgmLoop()`，重启关卡不再叠音。

### v2.3 (2026-08-11)
- **三项深层修复**：
  1. **BGM 舒缓重写** — 慢板 Am 五声音阶（~65 BPM, beat 0.92s），正弦波柔和起音（0.08s ramp），长衰减（0.92s），Am 和弦三音泛音铺底。不再急促，放松治愈。
  2. **螺丝逐层覆盖** — 底层网格散布 → 上层螺丝直接放置在下层螺丝正上方（偏移±6px），形成精准"遮盖链"。每颗上层螺丝都有对应下层目标，遮盖率 56-100%。
  3. **面板重叠加强** — 分布范围缩减（35%→22%），面板更集中，层间重叠增加。螺丝网格化散布（√N 网格+18%边距），同板螺丝最小间距 17-27px，不再挤在一团。

### v2.2 (2026-08-11)
- **三项用户反馈精准修复**：
  1. **BGM 彻底重写** — 音乐盒风格（C大调五声音阶 C5-C6 523-1047Hz），三角波短音（0.55s/拍，快速衰减），零低频，不持续不头疼。有效音量 11.2%→30.6%（note 0.4×bgmGain 0.9×master 0.85）
  2. **螺丝面板绑定** — 去掉二轮 ghost 浮层，恢复单轮逐层渲染。被遮挡螺丝画在所属面板层序内，上层面板自然遮盖下层 ghost 螺丝，不再浮到其他面板上方。
  3. **层位透明** — 最上层（未遮挡）螺丝正常颜色；被遮挡层螺丝半透明 ghost（alpha 0.28+🔒），只有当该层变成最上层时才恢复为正常色。

### v2.1 (2026-08-11)
- **三项核心修复**：
  1. **BGM 音量提升** — 主音 gain 0.05→0.2(×4)、底音 0.04→0.15(×3.75)、bgmGain 0.5→0.7，有效音量 2%→11.2%
  2. **Ghost 螺丝完整渲染** — 新增 _drawScrewRaw 画完整六角螺丝(渐变+高光+十字槽)，二轮 ghost 层 alpha 0.28
  3. **关卡策略重设计** — 盒子顺序先确定，首盒颜色螺丝强制放底层(被遮难拿)，tempMax 缩减(4-5)

### v2.0 (2026-08-11)
- **三项根因修复**：
  1. **物理方向修正** — 去掉 v1.9 错误加入的负号（`-currentAngle*0.5`），恢复 `atan2*0.55` 正方向。验证：右侧支点 → 重心左移下坠（CCW），左侧支点 → 重心右移下坠（CW），与重力一致。
  2. **BGM 固定序列** — 不再随机，改为 Am 五声音阶固定正反序列（110→130.8→146.8→164.8→196→164.8→146.8→130.8），每轮完全一致，确保优美连贯。
  3. **Ghost 极淡浮层** — 被遮螺丝改为二轮在最上层画色点（alpha 0.13 + 🔒），不遮盖上层木板的同时一眼可见不可操作螺丝的位置。

### v1.9 (2026-08-11)
- **四项精准修复**：
  1. **物理方向修正** — 改绕支点螺丝旋转(pivotX/Y)替代绕中心，重心被重力向下拉，不再上浮。与螺丝移除边的对侧会自然下垂。
  2. **BGM 完全重写** — 去掉所有效果器、打击乐、和弦，极简五声音阶正弦长音：5音符随机序列(+A2 持续低吟)，零噪音。
  3. **渲染恢复单轮层序** — 撤回二轮浮层，恢复面板+螺丝逐层画。被遮螺丝 ghost 扁平渲染(alpha 0.22 + 🔒图标)，不会跑到上层木板上面。
  4. **撤销物理状态保存更新** — offX/Y → pivotX/Y。

### v1.8 (2026-08-11)
- **5项用户反馈快速修复**：
  1. **BGM 去噪** — 白噪声沙锤 → 正弦短促打击(1800Hz/15ms/gain 0.015)，消除吱吱声
  2. **被遮螺丝真正半透明** — 两轮渲染：首轮画面板+可操作螺丝，二轮在所有面板上方画被遮螺丝(alpha 0.28+🚫)，浮于顶层
  3. **物理吸附消除浮动** — lerp 加 0.003 阈值吸附，接近目标直接归零；移除持续 wobble 动画
  4. **层次深度增强** — 阴影随层级加深(0.15+layer×0.035)，双层柔光；边框随层级更粗更暗
  5. **收纳动画提速** — 飞行 40→20 帧、等待 700→400ms、螺丝间隔 400→200ms

### v1.7 (2026-08-11)
- **5项全面重构**：
  1. **游戏结束逻辑修复** — 最后一颗螺丝填满盒子时不立即 autoMatch，让 `_autoClearTemp()` 统一处理收纳动画，玩家能看到暂存螺丝逐个飞入盒子
  2. **全新 BGM** — 五声音阶温暖氛围配乐（Am-Fmaj7-C-G-Dm7-Am-F-G7 八和弦进行），三角波低频+正弦和弦铺底+低通滤波低保真温暖感+五声音阶随机旋律装饰+柔和沙锤打击。参考真实 ScrewJam 的放松/ASMR 风格
  3. **螺丝遮挡可视化彻底改造** — 撤回半透明面板，改为面板不透明+被遮螺丝半透明(alpha 0.28)+🚫禁用图标。一眼区分可操作/不可操作
  4. **物理倾斜系统（全新）** — 螺丝移除后重新计算支撑：只剩1颗→木板绕该螺丝旋转倾斜（lerp 动画），2颗以上→稳定。面板掉落前先摇晃。倾斜/摇晃/掉落各有独立音效
  5. **非矩形面板形状** — 圆形/三角形/六边形/矩形混搭（第2关起引入），每形状独立 `shapePath()` 绘制和 `shapeContains()` 碰撞检测。难度大幅提升：面板更多(4-12块)+强力中心聚集+更多螺丝+更多重叠遮挡

### v1.6 (2026-08-11)
- **4项重大改进**（基于真实 ScrewJam 调研）：
  1. **BGM有效音量×7** — 多级增益重构：bgmGain 0.22→0.55, Master 0.65→0.8, Bass 0.15→0.35, Pad 0.05→0.12。有效值从2.1%→15.4%
  2. **收纳动画全程可见** — 飞行从20帧(333ms)延长到35帧(583ms)，螺丝间350ms停顿，显示"剩余X颗"进度
  3. **真正的 ScrewJam 难度** — 核心来自面板重叠遮挡，而非简单减暂存格。面板更大(75-150×55-110)，更多(3-10块)，中心聚集确保重叠
  4. **半透明面板+遮挡可视化** — 面板 alpha 0.62-0.93（可看穿），逐层绘制面板→螺丝，被遮螺丝暗淡+🔒锁图标，面板投射阴影

### v1.5 (2026-08-11)
- **3项用户反馈修复**：
  1. BGM音量大幅提升 — bgmGain 0.06→0.22，Master 0.5→0.65，各乐器分量翻2-3倍
  2. 自动收纳飞行动画 — 暂存螺丝像正常螺丝一样从临时区弧线飞入盒子，带拖尾粒子
  3. 难度显著提升 — tempMax 比 v1.4 减少 1-2 格，面板更集中重叠，关卡生成尝试次数 50→80

### v1.4 (2026-08-11)
- **5项重大改进**：
  1. Lo-fi 解压 BGM — 4和弦进行（Am-F-C-G）+ bass + pad + hi-hat + 木鱼
  2. 木板下落优化 — 速度减半、下落空间加倍、音效增强
  3. 结束自动清暂存区 — 所有面板清空后自动匹配暂存螺丝到盒子
  4. 难度曲线重写 — 1关极易→快速递增，贪心求解器保证可解（30关×3轮=90/90通过）
  5. 木板样式升级 — 8种木纹配色（橡木/蜂蜜/核桃/柚木/枫木/樱桃/松木/灰木）+ 木纤维纹 + 木节

### v1.3 (2026-08-11)
- **修复9个问题**：
  1. 移除木板掉落时的屏幕震动
  2. 修复空白木板bug（每块木板至少1颗螺丝）
  3. 木板支点旋转下落（替代滑动）
  4. 重来按钮现在重玩同一关（保存种子）
  5. 提示光环更大更明显（双层：金色+虚线）
  6. 主界面默认显示，不再直接进入关卡
  7. 主界面新增详细游戏规则说明
  8. 撤销时恢复面板状态（掉落、摇晃）
  9. 面板下落用蒙版裁剪替代渐隐

### v1.2 (2026-08-11)
- 动画全面升级 — 螺丝飞行弧线+圆角3D螺丝+面板圆角16px+震屏+连击+拖尾粒子

### v1.1 (2026-08-11)
- 微信浏览器全兼容 — WeixinJSBridge 音频解锁、X5 meta、全局触摸防护、安全区适配

### v1.0 (2026-08-11)
- 初版上线，6色体系，关卡生成器，音效特效完备

## 微信兼容（v1.1）

| 项目 | 方案 |
|------|------|
| 音频解锁 | WeixinJSBridge + touchstart 双重保险 |
| X5 浏览器 | x5-orientation/fullscreen/page-mode meta |
| 下拉刷新 | 全局 touchmove capture + CSS overflow:hidden |
| 长按菜单 | contextmenu preventDefault |
| 双击缩放 | dblclick preventDefault + maximum-scale=1.0 |
| 字体调整 | setFontSizeCallback 锁定 |
| 安全区 | viewport-fit=cover + env(safe-area-inset-*) |
| 横竖屏 | orientationchange 监听 + resize 触发 |
| 点击高亮 | -webkit-tap-highlight-color:transparent |
| 文本渲染 | -webkit-font-smoothing:antialiased |

## 待办

- 炸弹螺丝（限时螺丝）
- 冰冻螺丝（需解冻）
- 连击加分系统

## 验收规则（机械强制，不可跳过）

**每次修改代码后必须执行：**

1. 运行 `node .claude/verify.js`，全部 6 项检查通过才算完成
2. 如果某项失败，修复后重新跑，直到全部通过
3. **verification-gate Hook 会自动检查** `verify-results.json` 是否存在且字段完整，不满足则阻止结束

**同一问题修到第 3 次时：**
- 禁止直接改代码
- 必须先写验收契约（`.claude/contracts/` 下的 JSON），定义量化验收标准
- 用户批准契约后，才能动手
- 改完后 verify.js 的原始数据直接展示给用户，不允许只写结论

**验收数据不撒谎。** 遮盖率 11% 就是 11%，不能写成"遮盖良好"。
